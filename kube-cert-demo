server componsntes
kube-api-server  apisrver.crt and apisrevr.key
etcd server
kubelet server on worker node

client components
admin(us)
kube-schedulwr
kube-controller-manager
kube-proxy

kubeapi is only srver that talks to etcd so kubepai is client here


to cat the certificate to see detials
openssl x509 -in /etc/kuberenetes/mainfests/pki/apiserver.crt -yexy -noout
